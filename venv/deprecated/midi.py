import mido as mido #mido is a midi interfacing library
def plotMIDIdata(midiPath):
    # TODO: Find a way to plot midi data properly
    mid = MidiFile(midiPath,clip=True)
    return

def playMIDIdata(midiPath):
    # TODO find a way to play midi data

    return